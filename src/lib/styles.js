/** Shared Tailwind class strings used by more than one component. */

export const wrap = 'mx-auto w-full max-w-page px-gutter';

export const textButton =
	'cursor-pointer border-0 bg-transparent py-[0.35rem] font-semibold text-inherit underline decoration-2 decoration-gold underline-offset-[0.35em]';

export const lineInput =
	'min-w-0 flex-1 appearance-none rounded-none border-0 border-b bg-transparent py-[0.35rem] focus-visible:border-gold focus-visible:shadow-[0_1px_0_var(--color-gold)] focus-visible:outline-none';
